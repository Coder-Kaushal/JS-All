const btnRock = document.querySelector('.btnRock').addEventListener('click', playGame);
const btnPaper = document.querySelector('.btnPaper').addEventListener('click', playGame);
const btnScissors = document.querySelector('.btnScissors').addEventListener('click', playGame);
const btnRestart = document.querySelector('.btnRestart').addEventListener('click', restartGame);
const computerChoice = Math.random();





const playGame = () => {
    if()
}